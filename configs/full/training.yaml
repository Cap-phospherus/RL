training:
  algorithm: ippo
  episodes: 700
  rollout_steps: 256
  gamma: 0.99
  gae_lambda: 0.95
  clip_ratio: 0.2
  learning_rate: 0.0002
  hidden_dim: 128
  update_epochs: 4
  entropy_coef: 0.015
  value_coef: 0.5
  selection_eval_episodes: 5
  selection_interval: 50
  behavior_clone_episodes: 0
  behavior_clone_epochs: 0
  behavior_clone_learning_rate: 0.001
